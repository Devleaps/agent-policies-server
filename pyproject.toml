[project]
name = "re-internal-agent-policies"
version = "1.0.0"
requires-python = ">=3.12"

dependencies = [
    "fastapi>=0.140.13",
    "pydantic-settings>=2.14.2",
    "uvicorn>=0.51.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.1.1",
]

[tool.ruff]
exclude = ["mutants"]

[tool.mypy]
exclude = ["mutants"]
