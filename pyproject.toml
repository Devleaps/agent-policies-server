[project]
name = "re-internal-agent-policies"
version = "1.0.0"
requires-python = ">=3.9"

dependencies = [
    "devleaps-agent-policies",
]

[tool.uv.sources]
devleaps-agent-policies = { path = "../../DevLeaps/agent-policies", editable = true }

[project.optional-dependencies]
dev = [
    "pytest==8.4.2",
]

[tool.uv]
dev-dependencies = [
    "pytest==8.4.2",
]
